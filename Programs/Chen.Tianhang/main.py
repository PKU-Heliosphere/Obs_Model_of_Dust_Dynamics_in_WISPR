# 这是一个示例 Python 脚本。

# 按 Shift+F10 执行或将其替换为您的代码。
# 按 双击 Shift 在所有地方搜索类、文件、工具窗口、操作和设置。
import numpy as np


def print_hi(name):
    # 在下面的代码行中使用断点来调试脚本。
    print(f'Hi, {name}')  # 按 Ctrl+F8 切换断点。


# 按间距中的绿色按钮以运行脚本。
if __name__ == '__main__':
    print_hi('PyCharm')
    print(np.cos(np.pi/3))
# v = 10
# pho = 2.5e3
# r = 1e-6
# m = pho * 4/3 * np.pi * r**3
# B = 1e-2
# V_cloud = 2 * np.pi / 3 * (5*2.7)**3 * (1 - np.cos(1 / 9 * np.pi))
# mq = V_cloud / (0.043 * 25**3.46)
# r_p = v / B * 1.672623e-27 / 1.6e-19
# r_dust = v / B * 1.672623e-27 / 1.6e-19 * 1e7
# r_frag = v / B * m / 6.3e-17
# print(r_p)
# print(r_dust)
# print(r_frag)
# 访问 https://www.jetbrains.com/help/pycharm/ 获取 PyCharm 帮助

print(np.sqrt(1-0.16-0.585165**2))

